package commons

//MaxBatteryToReplace to replace {max-battery} of URI
const MaxBatteryToReplace = "{max-battery}"

//ActionToReplace to replace {action} of URI
const ActionToReplace = "{action}"

//VehicleIDToReplace to replace {vehicleid} of URI
const VehicleIDToReplace = "{vehicleid}"

//TicketIDToReplace to replace {ticketid} of URI
const TicketIDToReplace = "{ticketid}"

//TokenToReplace to replace {token} of URI
const TokenToReplace = "{token}"

//OffsetToReplace to replace {offset} of URI
const OffsetToReplace = "{offset}"
